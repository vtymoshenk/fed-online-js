function findMinNumber(firstNum, secondNum) {
    return Math.min(firstNum, secondNum);
}
console.log(`Min number is ${findMinNumber(6, 4)}`);

function findMaxNum(firstNum, secondNum) {
    return Math.max(firstNum, secondNum);
}
console.log(`Max number is ${findMaxNum(3, 87)}`);